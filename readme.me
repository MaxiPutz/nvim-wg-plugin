# nvim-add-peer

**nvim-add-peer** is a Neovim plugin that automatically creates new WireGuard peers directly from your editor.  
It generates client keys, updates your server’s `wg0.conf`, creates the client configuration folder, and copies the final client config to your clipboard (OSC52 or system `+` register).

Perfect for managing WireGuard servers while staying inside Neovim.

---

## ✨ Features

- 🔧 Automatically detects the next available peer number (`client1`, `client2`, …)
- 🔐 Generates WireGuard keypairs (`wg genkey`, `wg pubkey`)
- 📁 Creates a client directory and writes `privatekey`, `publickey`, and `wg0.conf`
- 🛠 Updates your server’s `wg0.conf` by appending the new peer block
- 📋 Copies the generated client configuration to your clipboard  
  - Uses **OSC52** (terminal clipboard) if enabled  
  - Falls back to Neovim’s `+` register otherwise
- 🚫 Safe defaults — clipboard copying is off unless explicitly enabled
- ⚡ Fast, clean, no external dependencies (optional OSC52 plugin)

---

## 📦 Installation

With **lazy.nvim**:

```lua
{
  "addPeerPlugin",
  dir = "~/workspace/addPeerPlugin", -- or your local path
  dependencies = { "ojroques/nvim-osc52" }, -- optional
  config = function()
    require("addPeerPlugin").setup({
      osc52 = false, -- default: use + register
    })
  end,
}

